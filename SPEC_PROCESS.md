# SPEC_PROCESS: Gopher 设计过程文档

> 记录使用 Claude Code（编码智能体）生成 SPEC 与 PLAN 的过程。

---

## 1. Brainstorming 关键节点

### 1.1 项目方向确认

对话开始时，我手里有三个文件：通用要求、A 类说明、B 类说明。我首先需要理解两类项目的区别。

**智能体追问的好问题：** 智能体先读了全部三个文档，然后清晰地总结了两类的核心区别——A 类是造一个 agent 的"内核"，B 类是造一个应用。这让我很快做出了选择。

**我的决策：** 选择 A 类（Coding Agent Harness）。理由：对底层机制更感兴趣，想理解 agent 到底是怎么工作的。

### 1.2 技术选型：语言

智能体给出了四种语言选项（TypeScript / Go / Rust / Python），并简要说明了各自优势。我让智能体自己决定。

**智能体选择：** Go。理由：单文件二进制分发最契合 GitHub Release 的分发方式，自带测试框架方便写 mock 测试，做 CLI 工具最顺手。

**我的态度：** 同意。Go 的编译产物确实最适合"下载就能跑"的场景。

### 1.3 技术选型：重点维度

六个维度（决策 / 工具 / 记忆 / 治理 / 反馈 / 配置）中必须选一个深入实现。智能体给出了四个适合"深入"的维度并推荐了反馈闭环。

**智能体选择：** 反馈闭环。理由：它是最能区分"真 agent"和"聊天机器人"的机制，且天然由确定性代码构成，最容易满足"移除 LLM 后仍可单测"的硬要求。

**我的态度：** 同意。反馈闭环的 mock 测试场景非常直观——注入失败 → agent 修正 → 再测——容易理解和验证。

### 1.4 智能体关于 CLI 分发的澄清

我向智能体转述了老师关于"如果只提供 CLI 且不想开发 WebUI，提供 GitHub Release 链接即可"的说法。智能体确认这对我有利：harness 天然是 CLI 工具，不需要硬造 Web 界面。

**这个澄清很关键——它影响了我后续对分发的设计。**

---

## 2. 三轮关键迭代

### 第一轮：从模糊到具体

**我的初始状态：** 只知道要做 A 类项目，其他完全没概念。

**对话节选：**
> 我："这个项目要给 harness 取名字吗"
> 智能体："不需要——但是项目总得有个名字，因为你需要建 GitHub 仓库、写 README 标题、填 submission.jsonc。叫个 Gopher 就行。"

**决策：** 采纳了"Gopher"这个名称，后续不再在命名上花时间。

### 第二轮：SPEC 草稿产出

**智能体的做法：** 基于我们确定的三个决策（Go + 反馈闭环 + CLI 分发），智能体一次性产出了一份完整的 SPEC.md，覆盖了课程要求的全部 10 节内容 + A 类额外的"领域与机制设计"一节。

**我注意到的事：** 智能体在没被明确要求的情况下，自动把 SPEC §3.6 的治理护栏补了完整的危险命令列表（rm -rf、git push --force、DROP 等），这比我想的更细致。

**我审阅后的修正：** 无重大修正。智能体产出的 SPEC 结构完整、模块清晰，我签字确认。

### 第三轮：PLAN 产出

智能体把 12 个 task 拆得很细，并画了依赖图。我注意到：

- **可并行标记**：智能体明确标了三组可并行执行的 task（T02+T03、T04+T05+T10、T06+T08），这在工作流中很有用。
- **预估时间**：总计约 5 小时（subagent 连续工作）。

**我的处理：** 接受 PLAN 作为实现蓝图。

---

## 3. AI 提出而我采纳的建议

| 建议 | 来源 | 采纳理由 |
|------|------|----------|
| 选择 Go 语言 | 智能体 | 单文件二进制分发最契合课程要求 |
| 反馈闭环作为重点维度 | 智能体 | 最容易写确定性测试，且最能体现 agent 的工程价值 |
| 项目命名为 Gopher | 智能体 | 简洁、与 Go 生态关联 |
| 治理护栏以正则模式匹配实现 | 智能体（写在 SPEC 中） | 简单可靠，确定性可验证 |
| 凭据存储降级方案（Linux/macOS 用加密文件） | 智能体（写在 SPEC 中） | 务实，承认跨平台困难但给出了渐进方案 |
| 死循环检测（同一动作连续 3 轮失败即停机） | 智能体（写在 SPEC 中） | 小机制但实用，防止 agent 原地打转 |

## 4. AI 提出而我修正或推翻的

| 建议 | 我做了什么 | 原因 |
|------|-----------|------|
| （本次 brainstorming 中，智能体的建议我全部采纳） | — | 智能体给出的建议都有明确的技术理由支撑，且我在每个决策节点都有机会提出异议，但判断其建议合理。 |

## 5. 反思：Brainstorming 过程的优劣

### 做得好的地方

1. **结构化追问**：智能体不是一次丢出所有问题，而是一个一个来——先语言，再维度，再命名。这降低了决策负担。
2. **每个选项都给了理由**：不是简单的"选 A 还是 B"，而是带着优劣分析，让人能做出有依据的判断。
3. **自动补全能力**：当我只给出了大方向（Go + 反馈闭环），智能体自动补全了大量细节（危险命令列表、JSON 解析方式、数据结构设计），这节省了大量"自己想"的时间。
4. **产出的 SPEC 直接可用**：不需要反复修改，结构就符合课程要求。

### 做得不好的地方

1. **缺乏真正的质询**：课程说 brainstorming 技能会"主动追问，任何含糊之处都应要求补足"，但实际上智能体几乎没有质疑过我的选择。当我两次说"你决定"，智能体就直接决定了，没有追问我的真实意图。
2. **过程太快**：整个 brainstorming 过程非常顺畅，但这可能不是好事——顺畅意味着双方都没有深入挖掘潜在的含糊点。真正的 brainstorming 应该有一些摩擦。
3. **没有充分探索替代方案**：比如选择了 Go，但没有深入讨论如果选 TypeScript 或 Rust 会有什么不同。"没有对比就没有选择"——虽然最终选择是对的，但论证不够充分。
4. **缺少对 SPEC 的迭代修改**：第一版 SPEC 产出后我直接接受了，没有经过"质疑→修改→再质疑"的循环。这可能意味着 SPEC 中隐藏了我还没发现的模糊之处——这恰好是冷启动验证要暴露的问题。

### 对冷启动验证的预期

基于以上反思，我预计冷启动验证中另一个 AI agent 会遇到的问题：
- 反馈闭环的 `go test -json` 输出格式可能描述不够具体（agent 不知道解析哪些字段）
- 治理护栏的正则列表可能不全（边缘情况未被定义）
- mock LLM 的脚本格式可能需要明确定义才能被另一个 agent 复现

这些预期将在冷启动验证中被证实或推翻。

---

## 6. 冷启动验证记录

**时间：** 2026-08-10

**验证 agent：** Claude Code subagent（全新会话，无任何对话历史，仅提供 SPEC.md + PLAN.md 全文）

**指定任务：** PLAN T06（治理护栏）

### 6.1 执行过程

Subagent 收到 SPEC + PLAN 后，自主完成了以下步骤：
1. 下载 Go 1.24.4 工具链（本机未安装）
2. 初始化 Go module（`github.com/gopher/gopher`）
3. 创建 `internal/tools/tool.go`（因为 T06 依赖 T03 的 `ToolCall` 类型）
4. 编写 `internal/guard/guard_test.go`（11 个测试用例）——**红色阶段**
5. 实现 `internal/guard/patterns.go` + `internal/guard/guard.go` ——**绿色阶段**
6. 运行 `go test`、`go vet`、`go build`、`gofmt` 全部通过

### 6.2 Agent 提问与暂停

Agent 在执行中主动提出了以下问题：

> **关于 Module path：** SPEC 使用占位符 `github.com/<user>/gopher`，但 `<`/`>` 是 Go module path 的非法字符。Agent 自行使用了 `github.com/gopher/gopher`，并在最终报告中标注"Replace with the real GitHub username before publishing"。

> **关于 `run_shell` 参数 key：** SPEC 未定义 shell 命令的参数键名。Agent 兼容了 `command`/`cmd`/`script`/`shell`/`input` 五种可能的键名，并对于 `run_shell` 工具回退为拼接所有字符串参数。

> **关于白名单语义：** SPEC 说"可通过配置文件将特定命令加入白名单"，但未说明匹配规则。Agent 选择了前缀匹配（whitelist `rm` 则所有 `rm` 命令都放行），并在报告中请求确认。

### 6.3 暴露的 SPEC 缺陷

| # | 缺陷 | 严重程度 |
|---|------|----------|
| 1 | Module path 使用占位符 `<user>`，在 Go 中非法 | 中 |
| 2 | `ToolCall.Args` 的参数键名未定义（`command`？`cmd`？`path`？） | **高** |
| 3 | 白名单匹配语义未明确（精确匹配 / 前缀匹配 / 正则？） | 中 |
| 4 | Go 版本未指定（Agent 默认选 1.22 → 实际下载了 1.24.4） | 低 |

### 6.4 产出质量评估

Agent 产出的代码：
- **正确性：** 11/11 测试通过，`go vet` 零告警
- **完整性：** 覆盖了 SPEC §3.6 列出的全部 9 类危险命令
- **安全性：** 严格模式匹配，无遗漏、无假阴性
- **与 SPEC 预期的一致性：** 基本一致，偏差仅来源于 SPEC 未定义的部分

### 6.5 据此对 SPEC/PLAN 的修订

**SPEC 修订（修订前后 diff）：**

1. §8.1 技术选型：Module path 从 `github.com/<user>/gopher` → `github.com/ztjd/gopher`（使用我的实际 GitHub 用户名）
2. §6.2 数据模型：`ToolCall.Args` 补充参数键名约定（`path` 用于文件工具，`command` 用于 run_shell）
3. §3.6 治理护栏：白名单语义补充为"前缀匹配"
4. §8.1 新增 Go 版本要求：`go 1.22+`

**PLAN 修订：**
- T03（工具系统）实现要点新增：`run_shell` 的参数键为 `command`

---

*记录时间：2026-08-10，冷启动验证完成。*
